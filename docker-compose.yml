version: '3.8'
services:
  frontend:
    build: .
    ports:
      - '5174:80'
    restart: unless-stopped
  backend:
    build: ./server
    ports:
      - '4000:4000'
    environment:
      - PORT=4000
    restart: unless-stopped
